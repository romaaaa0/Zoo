using System.Collections.Generic;
using System.Text;
using UnityEngine;

namespace Codes.John.Steave
{
    public class Zcwiwxpj : MonoBehaviour
    {
        private void Awake()
        {
            DontDestroyOnLoad(this);
        }

        public string wldkakd(List<string> lslscvplo)
        {
            StringBuilder hflslfs = new StringBuilder();
            foreach (var s in lslscvplo)
            {
                hflslfs.Append(s);
            }

            return hflslfs.ToString();
        }
    }
}